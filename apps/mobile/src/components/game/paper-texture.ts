// Reuse the exact cotton-paper texture from the public web page so both
// products share the same material language without duplicating the asset.
export const PAPER_TEXTURE_SOURCE = require("../../../../web/public/images/paper-cotton.jpg");
